[tool:pytest]
addopts = -q --doctest-modules --ignore=setup.py --ignore=bootstrap.py --cov=openprocurement.auction.worker --cov-report term-missing
python_files = *test_*.py
testpaths = openprocurement/auction/worker/tests/unit/

